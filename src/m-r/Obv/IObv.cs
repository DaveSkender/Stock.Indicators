namespace Skender.Stock.Indicators;

/// <summary>
/// Interface for On-Balance Volume (OBV) calculations.
/// </summary>
public interface IObv
{
    // OBV doesn't have parameters like lookback periods, so interface is minimal
}