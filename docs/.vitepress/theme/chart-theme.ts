// Shared chart theme constants. Single source of truth for indy-charts
// annotation/axis-label backdrops so the VitePress theme switcher and the
// landing overlay stay in lockstep. Matches the `--vp-c-bg` values defined
// in `custom.scss`.

export const DARK_SURFACE = '#22272e'
export const LIGHT_SURFACE = '#f3f4f6'
